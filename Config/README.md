Configuration files and examples go here. Recommended layout:

- config/config.yaml - runtime configuration (sensitive values via env)
- config/logging_config.yaml - logging settings

Do not commit secrets to this folder.
